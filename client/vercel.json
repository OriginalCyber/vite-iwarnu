{
    "buildCommand": "cd client && npm install && ./node_modules/vite.js build",
    "outputDirectory": "client/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/server/(.*)",
            "destination": "/api/index.cjs"
        }
    ]
}